# 2026-08-23 — UD-1 answered: the D-426 staging deploy (gap closed at 21 commits)

Historical narration only — non-authoritative (DQ-1). Current state: `docs/PROJECT_STATE.md`;
the judgment record is **D-426** in `docs/DECISIONS.md`; the deploy's own evidence is GitHub
Actions run 32613654181.

**What happened.** The user answered UD-1 ("let's deploy", 2026-08-23) — Option A. The
coordinator executed it directly (an operational trigger, per D-417 §C9's manual-only design;
no Orca executor — nothing to implement). Sequence:

1. **Pre-deploy obligations** (UD-1's carry-regardless list): D-426 written first, recording
   the LB-08 baseline (10.55 s guest-QA on `gha-44a12dfc9549`; D-423's own 10.62 s) before the
   deploy made that build unreproducible, and re-homing the criterion-6 gate-integrity
   sub-question with the defensible default (week counts from the first real `JobCompletions`
   datapoint → earliest 2026-08-29). Pre-deploy readings: learning `:150` 2/2 / chat `:148` 1/1
   on `gha-44a12dfc9549`; `learning_checkpoint_repairs_total` 0.0 flat over 14 days (§7-R9
   tripwire baseline); heartbeat alarms 3 OK + `memory-consolidate` ALARM (accurate signal).
2. **Deploy:** `gh workflow run deploy-staging.yml --ref main` at `898e2fb` (product code
   `67cd708`). Run 32613654181, conclusion **success**, ~03:05Z: images built and pushed,
   ops-task patched, **Alembic migrations applied** (incl. `8509c0486d8d` →
   `chat_escalation_sends` exists; `WORK-03` closed; D-421's duplicate-send guard live),
   curriculum/bank load, MySQL dev-fake re-seed, RAG re-embed, suggestion seed, both services
   deployed, deployed-version gate passed, `/dev/token` edge gate passed, canary bake clean
   (rollback skipped), SPAs synced + CloudFront invalidated, smoke tests through the public
   CloudFront URLs passed.
3. **Independent post-deploy verification (coordinator):** both services on
   `gha-898e2fb4270b` (learning `:152` 2/2, chat `:150` 1/1); repair counter still 0.0 through
   the task drain — the §7-R9 acceptance is intact; COST-22's pre-initialised series confirmed
   live (`qa_service_degraded_total{stage=scope_guard|document_qa_retrieval|calendar_retrieval}`
   in the deployed chat-api namespace); heartbeat alarms unchanged.

**What this deploy shipped** (the 21-commit gap, all previously CI-only): the B4 escalation
series (D-420/421/422), C8, D-423's docs, and every 2026-08-21/22 Orca-run fix — REQ-27 pins,
the RD-01 parity test, COST-22 pre-init, the SEC-13 cancel/exception purge, COST-06 spend
accounting, the RD-01 weekly-window cap, span-event redaction, the synthesis-time
approved/effective predicate, and the total reason sweep.

**Reconciliation applied.** DECISIONS: D-426 appended (the UD-1 answer, the baseline, the
sub-question default, pre/post-deploy readings). PROJECT_STATE: §1 snapshot (deployed image
`gha-898e2fb4270b`, task defs `:152`/`:150`, gap **none**), §3 rewritten from "the deploy gap"
to "gap closed" with the surviving facts (B4 deployed-but-unobserved, LB-08 → D-426, COST-22
live, §7-R9 held, the SPA artifact-freshness gap unchanged), §5 UD-1 row deleted and the
sub-question re-homed note updated (four hanging items → three), `WORK-13-FIXTURES`'s UD-1
ordering constraint discharged, `DB-CONTENT-VERIFY` narrowed (WORK-03 closed). Register:
dated UD-1-answered annotation on `LB-05-DEPLOY-GAP` (the artifact-freshness half stays open).

**Unresolved handoff items.** (1) B4's live behaviour is still unobserved — the Option-A
re-walk belongs to a future live-probe session (each staging turn is a paid Bedrock call,
UD-2/UD-3 context). (2) `DRIFT-24-ARTIFACT-FRESHNESS` and the freshness-mechanism question
remain open. (3) Queue row 1 is still RD-01's Sunday 2026-08-24 18:30 UTC confirmation.
(4) The e2e single-spec isolation claim now describes a no-longer-deployed build
(`WORK-13-FIXTURES` restated accordingly).

**Commit/SHA.** Deployed: `gha-898e2fb4270b` (git `898e2fb`, product code `67cd708`).
Docs-only reconciliation in this session's PR.
