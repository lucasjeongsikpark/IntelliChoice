# 2026-08-21 — RD-01 fixed and applied live; COST-22 resolved (second Orca coordinator/executor run)

Historical narration only — non-authoritative (DQ-1). Current state: `docs/PROJECT_STATE.md`;
durable evidence: `test_scheduled_job_event_parity.py`, `test_metrics_label_preinitialisation.py`,
git history, and the live staging metric filters. No new judgment was made, so no DECISIONS entry
exists for this session.

**What was done.** Second run of the Orca coordinator/executor workflow, on PROJECT_STATE §4.4
queue row 1 (`RD-01` + `COST-22-LABEL-PREINIT`, the "silent instrument" batch). The coordinator
verified the eligibility gate against the register and primary evidence, froze the task as
`tasks/rd-01-cost-22-silent-instruments.md`, and dispatched it through Orca
(run `run_e932f123adb1`, task `task_945af7cdf6ab`, dispatch `ctx_1e6bf604aba6`) to a persistent
executor launched as agent `claude`, model `opus`, effort `high` — requested and effective
identical in the launch receipt. The executor delivered the terraform-side pattern fix, the
D-385-class parity test (verified failing pre-fix), the import-time label pre-initialisation of
all eight bounded labelled metrics, and the completeness guard.

**One correction round.** The coordinator's review found one factual error repeated in two
durable comments: the claim that an emitter-side event-name fix "would have broken the dimension"
(false — the event name and the `extra={"job": job}` field are independent; the real
discriminators were deploy-cost asymmetry and the underscored structured-event convention). The
same persistent executor corrected both passages (task `task_c1d5b2021773`, dispatch
`ctx_49a0700f86a0`); the RD-01 commit message carrying the same claim was reworded by the
coordinator during cherry-pick.

**Evidence produced.** Parity test failed pre-fix (2 failed / 1 passed) and passed post-fix;
one-sided emitter mutation and pre-init-removal mutation each killed and reverted; focused
observability package 54 passed; `make lint`/`typecheck`/`test` exit 0 (executor:
1756 passed / 3 skipped / 1 xpassed). The coordinator independently re-ran the focused tests,
a terraform-side mutation kill (revert pattern → 2 failed → restore), lint, and typecheck
before accepting.

**Landing and apply.** Worktree commits cherry-picked to `land/rd01-cost22-silent-instruments`
(trees verified byte-identical), landed by PR #362 into protected `main` as `b06a5df`
(RD-01 fix + parity test), `4dbcc41` (COST-22 pre-init + completeness test), `bb75829`
(comment correction) after all 12 CI checks passed. The coordinator then ran the targeted
`terraform plan` (`-target=module.observability.aws_cloudwatch_log_metric_filter.nightly_jobs`,
exactly `0 add / 4 change / 0 destroy`) and applied it; all four live filter patterns on
`/ecs/intellichoice-staging-ops-task` read back underscored the same day.

**Reconciliation applied after acceptance.** PROJECT_STATE: snapshot header advanced to
`bb75829` (gap 14 commits), `COST-22-LABEL-PREINIT` row deleted (counts 26→25, 16→15), `RD-01`
restated to its time-blocked confirmation, §4.4 queue reordered with a dated reason
(`SEC-13-PURGE` now #1), §3/§6.2/§6.4/§8 fan-outs reversed or restated. ARCHITECTURE: the two
RD-01 passages updated with the dated fix, and a COST-22 lesson added (including the 34-series
upper bound and the `sse_relay_failures_total` cannot-be-alarmed observation). Register: dated
partial annotation on `RD-01` (steps a/b/d done, c pending) and a resolved-in-repo annotation
on `COST-22-LABEL-PREINIT`. DECISIONS and TRACEABILITY: untouched (no new judgment; no owed
rows). The Frozen Spec was deleted after reconciliation.

**Unresolved handoff items.** (1) RD-01 step (c): after the first post-fix nightly firing
(earliest 2026-08-22 ~19:00 UTC), confirm `JobCompletions` publishes and the four alarms
transition ALARM → OK — queue row 2. (2) The COST-22 series reach staging only at the next
image deploy (UD-1); the 34-series upper bound should be acknowledged when UD-3/COST-25 cost
posture is next discussed. (3) `worker-start --task <id> --terminal <handle>` requires the
`--worktree` flag too when the coordinator sits in a different worktree — orchestration
ergonomics note only.

**Commits/SHA.** Worktree `d09b44a`/`069396a`/`3c1f777` on
`lucasjeongsikpark/rd01-cost22-silent-instruments`; landed as `b06a5df`/`4dbcc41`/`bb75829`
by PR #362.
