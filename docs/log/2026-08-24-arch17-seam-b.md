# 2026-08-24 — standalone session: seam (b) healed, WORK-24 refuted (D-432)

> Non-authoritative narration (DQ-1). Current state: `docs/PROJECT_STATE.md`. Judgment: D-432.

## What was done

Queue row 2 (`ARCH-17-COMMIT-SEAM` + `WORK-24-DUPLICATE-GAIN`, taken as the coupled pair;
`RD-01` still time-blocked until 18:30 UTC today).

- **Counter read first, as mandated**: `learning_checkpoint_repairs_total` is 0.0 for every
  daily datapoint 2026-08-10 → 2026-08-24 in both namespaces, and has zero datapoints before
  (pre-COST-22 the metric only published on increment, so absence = zero increments). §7-R9
  intact by its own terms. Read 2026-08-24 UTC against `gha-898e2fb4270b`.
- **Seam (b) — the mid-interrupt half of AUD-X-07 — now heals** inside the resumed
  `intervention_choice` node (`graph/nodes.py`): on a checkpoint that promises a
  `study_attempts` row the domain commit discarded, the node completes with a backward
  repair — pause cleared, attempt id cleared, growth-oriented re-answer message, question
  re-rendered from the S23-preserved `last_items`. Counts on the same §7-R9 tripwire
  counter; runs before any Bedrock call. `checkpoint_reconcile.py`'s seam-(b) comment
  reconciled (it also pointed at the archived `PROGRESS.md`).
- **New test** `test_checkpoint_reconcile.py::test_b_…`: real induction (`_publish_snapshot`
  raising between the two commits on a wrong study answer), asserts the 409-blocked dead-end
  shape, the healing `/respond` (200, counter +1, pause cleared), and a successful fresh
  re-answer. Also documents that `/resume` 200s during a pause by design.
- **WORK-24 closed — hypothesis refuted by mechanism**: the seam discards a turn's domain
  half, so it cannot double-commit one; a duplicate `learning_gain` row needs two committed
  transactions — exactly D-336's no-idempotency finalize retry. D-336's guard
  (`record_learning_gain` returns the existing `(pre, post)` row) and constraint
  (`uq_learning_gain_pre_post_cycle`, migration `ecee04921753`) verified present in code.
- **§7-R9 narrowed** (not re-decided): its subject is now the commit ordering only; terms
  unchanged (movement voids; alarm question stays with UD-5).

## Evidence / verification

- `make lint`, `make typecheck` green; `make test` **1779 passed, 2 skipped, 1 xfailed**
  (455 s) — one more pass than the previous baseline: the new seam-(b) test.
- The healing is verified locally end-to-end via the induced crash; **not yet deployed** —
  staging still runs `gha-898e2fb4270b`, where seam (b) is still a dead end until the next
  deploy (LB-05).

## State changes

- `ARCH-17-COMMIT-SEAM` and `WORK-24-DUPLICATE-GAIN` deleted from §4.1 and the queue
  (`D329-PHANTOM` is now row 2); §6.4's R9 block restated to the narrowed subject;
  ARCHITECTURE.md's seam bullet updated (both seams heal; ordering open under R9).

## Handoff

- Earlier this session (same working day): D-428 runbook fix, D-429 WORK-44 closure, D-430
  dependabot batch merge, D-431 python-pin — landed via PR #379.
- Next: `RD-01`'s confirmation after today's 18:30 UTC run; otherwise queue row 2
  (`D329-PHANTOM`).
- The seam-(b) fix reaches staging only at the next deploy (manual trigger, D-417 §C9).
