# 2026-08-24 — ninth Orca run: D329-PHANTOM closed (D-433)

> Non-authoritative narration (DQ-1). Current state: `docs/PROJECT_STATE.md`. Judgment: D-433.

## Workflow

Orca coordinator/executor per CLAUDE.md: coordinator investigation → Frozen Spec
(`tasks/d329-detection-gap.md`) → run `run_dd542e45546b`, task `task_87f8d090c4a1` → one
persistent executor, launch receipt requested = effective = claude/opus/high (dispatch
`ctx_b6a9b68b51f8`) → worker_done → coordinator diff review → one correction round to the same
terminal (metric rename, dispatch `ctx_f056e8268c08`) → independent re-verification →
acceptance → this reconciliation. Worker released after settlement; task artifacts under
`tasks/` deleted after this reconciliation.

## What was done (executor-implemented, coordinator-accepted)

- `learning_hint_personalization_outcomes_total{outcome}` — six pre-initialised outcomes,
  incremented exactly once per task run structurally (`_run` counts the typed `_Outcome` that
  `_personalize` returns). The alarmed raising channel (`background_hint_personalization_failed`
  → wildcard filter → `BackgroundTaskFailures`) is byte-identical. Detection shape: `hint`
  support usage moving while `published` stays flat = personalization running dead (D-329's
  actual failure mode, previously invisible).
- **Prometheus-only, by boundary**: EMF promotion needs the otel allowlist in terraform, which
  the spec forbade — the executor returned this as drift instead of resolving it. Promotion
  (+6 learning-api series) now rides UD-5/COST-25 as a recorded sub-question.
- `test_stream_personalized_hint_over_http.py`: first real-uvicorn harness in the suite;
  proves canonical-then-personalized frame ordering on a held-open HTTP SSE stream, pinned to
  the counter and to the `hint_events` row; falsified by removing the scheduler injection.
- Register action (3) (the D-329 phantom id) closed by citation: H4's table has recorded the
  absence since 2026-08-20.

## Verification

- Executor: full suite 1784/2/1 twice (pre- and post-rename, 467.72 s / 474.73 s; baseline
  1779 + exactly the 5 new tests); ruff/pyright clean; the two changed test files 10 passed /
  0 skipped; SSE test 4 consecutive clean runs.
- Coordinator (independent): full diff read; rename grep-zero outside `tasks/`; 73 focused
  tests green; lint/typecheck green. All local; staging unchanged (`gha-898e2fb4270b`).
- Mid-task scare: a whole-suite run projected ~3.5 h; coordinator guidance (leak diagnosis or
  evidence of pre-existing drift) was followed and the final full runs came in at ~8 min. The
  executor's environment note attributes the likely cause to local dev-DB bloat (6.39M
  `checkpoint_writes` rows / 3.1 GB, autovacuum last 08-15..18) — environment observation only.

## State changes

- `D329-PHANTOM` deleted from §4.1 and the queue (`D356-FAMILY` is now row 2); UD-5 gains the
  EMF-promotion sub-question; ARCHITECTURE's COST-22 paragraph now counts nine pre-initialised
  metrics / 26 combinations and states the counter's Prometheus-only status; the silent-failure
  corollary bullet gains the ran-dead fourth corollary.

## Handoff

- `RD-01` confirmation after today's 18:30 UTC run remains queue row 1.
- The EMF promotion question and the alarm posture stay with the user (UD-5/COST-25).
- A staging walk of the personalized-hint path belongs to the next live-probe session.
