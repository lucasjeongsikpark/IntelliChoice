# 2026-08-26 — the D-448 deploy: 34 commits ship, the gap closes to zero

> Non-authoritative narration (DQ-1). Current state: `docs/PROJECT_STATE.md`. Judgment: D-448.

## What was done

On the user's "go deploy": `gh workflow run deploy-staging.yml --ref main`, run **32930929448**
pinned by head SHA `5fa15d4` (product code `7983154`). All gates green — deployed-version,
`/dev/token` edge, canary bake (rollback skipped), the D-439 blocking image-consistency gate's
first real-deploy firing, SPA syncs + invalidations, CloudFront smoke. Both services verified
on `gha-5fa15d491057` post-drain (learning `:153` 2/2, chat `:151` 1/1). No migrations in the
window.

## Verification

- Pre-deploy tripwire: `learning_checkpoint_repairs_total` 0.0, both namespaces, 3-day window.
- Post-deploy tripwire: 0.0, both namespaces, 4 h window / 16 datapoints, read 04:58Z.
- ECS read-back of images and revisions; run watched to completion, not "latest is green".

## Now live for the first time

The D-324 date-zone pair, D-432 seam-(b) healing, D-433 outcome counter (Prometheus-only,
UD-5), D-441 fan-out + D-440 input bounds, DRIFT-91, 22 dependabot bumps, D-447 defaults.

## Deliberately unmeasured

LB-08's post-optimisation comparison is newly *measurable* but paid (UD-2; use D-441's span
mapping vs the 10.55 s baseline). B4 escalation: still never observed live. DRIFT-24's SPA
freshness gate class: still absent.

## Handoff

UD-2 (now also the LB-08 measurement window), UD-13, UD-5's EMF promotion, D310 (a), and the
2026-08-31 07:17 UTC scheduled-controls check — which will take its first look at
`gha-5fa15d491057`.
