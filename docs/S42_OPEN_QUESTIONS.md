# S42 — 아직 알아내야 하는 것들 (추적 목록)

**기준일:** 2026-08-01 (D-151 이후) · **근거:** [S42_DISCOVERY.md](S42_DISCOVERY.md)

소스로 답할 수 있는 건 [S42_DISCOVERY.md](S42_DISCOVERY.md)에서 이미 답했습니다. 이 문서는
**소스로 답할 수 없어서 남은 것만** 담습니다. 답이 나오면 이 문서에서 지우고 DECISIONS.md로
옮깁니다.

## ⛔ 이 목록은 D-152로 **동결**되었습니다 (막힌 게 아니라, 미룬 것)

사용자 결정: **현재 코드베이스를 fake 위에서 완성·테스트한 뒤에 통합한다.** 따라서 아래
**A·B·C·D 그룹은 통합 착수를 선언하기 전까지 열지 않습니다.** 지금 측정해봐야 몇 달 뒤엔
낡은 값이 되고, auth 결정은 그 측정 위에서만 의미가 있습니다.

### ✅ 2026-08-02 사용자 답변으로 해소된 항목 (D-153)

- **C3(DNS)** — **가능하다고 확인됨. 조직이 통합 시점에 추가해줌** → 더 이상 미결 아님
- **C1·C2(타임존)** — **증거로 종결.** 공개 스케줄이 월–금 10:00–12:00 / 18:00–20:00이고
  일요일 세션도, 자정~01:00 세션도 없음 → 두 규약이 날짜·주 모두 동일한 값을 냄.
  Message A는 blocker가 아니라 확인 차원의 질문으로 격하 (D-153 §4)
- **C8(최대 동시 사용자)** — 미측정이나 **계획 가정 확정: 1주에 걸쳐 ~1,000명**
- **E1·E2·E3·E4 전부** — 사용자가 기존 시스템 관리자에게 수정 요청 예정 (E2는 아래 정정 참조)

> ⚠️ **E2 정정 (D-153 §7).** 처음엔 "role 자가 할당을 그대로 둔다"로 기록했으나, 사용자 정책은
> **Student/Parent/Tutor는 본인 선택, `Manager`는 관리자만 부여**입니다. 확인 결과 **의도는
> 실재하고 절반만 구현되어 있으며, 빠진 절반이 강제하는 쪽**입니다 — 회원가입 폼은 라디오 3개로
> 정책을 지키지만, API(`account.controller.js:26,33`)는 `req.body.role`을 그대로 저장합니다.
> role 변경 엔드포인트는 아예 없어서 `Manager`는 DB 직접 편집으로만 부여됩니다.
> **두 번째 경로도 발견:** `:56` — 미인증 계정에 같은 이메일로 재가입하면 **비밀번호와 role이
> 덮어써집니다.**
> **수정은 작습니다:** create에서 `Parent`/`Student`/`Tutor` allowlist, 그 외 400;
> 미인증-중복 분기에서는 role을 아예 받지 않기. `Manager`는 지금처럼 DB/관리자 작업으로 유지.
> **⛔ 하지만 그 수정이 들어와도 우리 쪽 제약은 그대로입니다:** 수정 이전 행이 이미 자가 할당된
> `Manager`를 갖고 있을 수 있고, 프로덕션은 동결·스키마 드리프트 상태라 수정이 유지되는지
> 단언할 수 없으며, 무엇보다 **권한 판정을 다른 시스템의 입력 검증에 위임하지 않습니다**
> (CLAUDE.md 규칙 3). **S43/S44: 프로덕션 role만으로 상위 권한을 주지 않는다** —
> Student/Parent는 매핑하되 `Tutor`/`Manager`는 새 스택이 관리하는 allowlist로 재확인.

**아직 유효한 예외:**
- **E그룹 통지** — 이 프로젝트 일정과 무관하게, 오늘 실사용자를 받는 시스템의 문제

아래 긴급도 표기는 **통합을 착수한 시점 기준**의 상대 순서입니다.
🔴 착수 즉시 · 🟡 S44 전 · 🟢 파일럿(S51) 전

---

## A. 사용자가 지금 바로 답할 수 있는 것

| # | 알아야 할 것 | 왜 | 긴급도 | 지금 없으면 |
|---|---|---|---|---|
| **A1** | **production API의 실제 base URL** | 소스에 커밋된 값은 `http://localhost:8080/api`가 유일 — 실제 주소를 모르면 아무것도 측정할 수 없음 | 🔴 | B 그룹 전체가 막힘 |
| **A2** | **테스트 계정 1개** (이메일+비밀번호) | server-to-server 로그인/`signups` 응답을 실데이터로 검증. 실계정이 부담되면 조직에 테스트 계정 요청 | 🔴 | B2·B4 불가, 계약 검증이 소스 추론에 머무름 |
| **A3** | API가 `go.intellichoice.org`와 **같은 호스트인지 별도인지** | CORS가 `localhost:3000`으로 하드코딩되어 있어 프록시/동일 출처 구성이 있어야 함 — 토폴로지 추정의 출발점 | 🔴 | B1 측정 설계가 불명확 |
| **A4** | **[결정] auth 옵션** — O1b 권장 | S44의 게이트 (INTEGRATION_PLAN §3.1) | 🟡 | S44 착수 불가. S43은 무관 |
| **A5** | **[결정] 타임존 규약** — 현재 default 유지 권장 | 되돌리기가 env var 3개로 확인됨 → 지금 확정하고 나중에 수정해도 안전 | 🟢 | 없음 (default가 이미 안전) |

## B. 측정으로 답할 수 있는 것 — A1·A2가 있으면 내가 실행

조직 답변을 기다리지 않고 **증거로** 답하는 항목. Message C의 상당 부분을 대체합니다.

| # | 측정할 것 | 왜 | 긴급도 |
|---|---|---|---|
| **B1** | **AWS에서 icrest에 도달 가능한가** (HTTPS, 방화벽/IP allowlist 유무) | **O1b 성립의 전제.** 실패하면 즉시 O2로 전환 → DB 접근 요청(C4·C9)을 지금 시작해야 함 | 🔴 |
| **B2** | 로그인/`signups` 응답이 이 checkout과 **일치하는가** | 배포된 빌드 == 소스 확인. 다르면 S42_DISCOVERY의 계약 전체가 재검증 대상 | 🔴 |
| **B3** | 로그인 경로 **지연시간** (p50/p95) | 로그인이 사용자 체감 경로에 들어감. criterion 7 임계값과 같은 성격 | 🟡 |
| **B4** | `signups` 응답에 **실제로 `attended`가 실려 오는가** | 소스로는 확인 완료(§2). 실데이터로 한 번 더 — 이 프로젝트의 "측정 전 통제" 규칙 | 🟡 |
| **B5** | 응답 헤더로 **프록시/WAF 존재** 추정 | §6.1(DoS)·§6.6(`?token=` 유출) 심각도가 여기서 갈림 | 🟡 |
| **B6** | `GET /` 주기적 샘플링 → **가용성 관찰** | C6(장애 이력)을 기억이 아닌 관측으로 일부 대체. 단 `/`는 DB를 안 건드리므로 liveness만 | 🟢 |

## C. 조직에만 물을 수 있는 것 (Message A~D)

| # | 질문 | Message | 왜 | 긴급도 |
|---|---|---|---|---|
| ~~**C1**~~ ✅ **종결** | 타임존 규약 결정 + **조직이 정말 US Central인가** | A | 소스가 두 규약을 동시에 구현 → 판정 불가 · **[2026-08-20 주석 · D-153 §4]** 증거로 종결. `ORG_TIMEZONE=America/Chicago`는 소스로 확인됨(S42_DISCOVERY §5), Message A는 blocker가 아니라 확인용 질문으로 격하. 아래 "해소된 항목" 원장과 동일 | ~~🟢~~ → 해소 |
| ~~**C2**~~ ✅ **종결** | **일요일 저녁 / 00:00~01:00 세션이 있는가** | A | 두 규약이 *주 경계*에서 갈리는 유일한 구간. 출석한 학생이 잘못 차단될 수 있음. "없다"면 그것으로 완결 · **[2026-08-20 주석 · D-153 §4]** 공개 스케줄이 월–금 10:00–12:00 / 18:00–20:00 뿐이라 **"없다"로 완결**. 즉 두 규약이 날짜·주 모두 같은 값을 냄 | ~~🟢~~ → 해소 |
| ~~**C3**~~ ✅ **종결** | **DNS 추가 가능 여부** (`learning.`, `chat.`, 메일용 1개) | B | ~~**미룰 수 없음** — 순수 리드타임, 미룬 만큼 S48이 밀림~~ · **[2026-08-20 주석 · D-153]** **조직이 이미 답했습니다: 가능하며 통합 시점에 추가해줌.** 따라서 위의 🔴 "미룰 수 없음" 표기는 **낡은 것**이며, 지금 보낼 ask가 아닙니다. 리드타임 논거 자체는 통합 재개 시 다시 유효 | ~~🔴~~ → 해소 |
| **C4** | MySQL(`ic`)이 어디서 돌고 **AWS에서 접근 가능한가** | C | O1b면 critical path 밖. **O2 fallback에는 필수** | 🟡 |
| **C5** | **TLS를 무엇이 종단하는가** (프록시/WAF/게이트웨이) | C | B5로 부분 추정 가능하나 확정은 조직만 | 🟡 |
| **C6** | 관측된 **장애 이력** | C | 코드에 모니터링이 전혀 없어 기억이 유일한 기록 | 🟡 |
| **C7** | **stdout 로그가 어디로 가고 누가 보는가** | C | §6.3(로그에 이메일+비밀번호 해시) 심각도가 전적으로 여기 달림 | 🟡 |
| ~~**C8**~~ ✅ **종결(가정 확정)** | **최대 동시 사용자 + 피크 시간대** | D | capacity sizing. 150 동시 목표는 자체 설정값이고 실측은 ~25 · **[2026-08-20 주석 · D-153]** **미측정이지만 계획 가정이 확정됨: 1주에 걸쳐 ~1,000명.** 조직에 물어서 열 항목이 아니라, 확정된 가정으로 sizing하면 되는 항목 | ~~🟢~~ → 해소 |
| **C9** | **read-only DB 계정** | C | D 그룹 실행에 필요. O2 fallback에는 필수 | 🟡 |

## D. 라이브 DB를 읽어야 답이 나오는 것 (C9 필요, 또는 조직이 대신 실행)

| # | 읽을 것 | 왜 | 긴급도 |
|---|---|---|---|
| **D1** | `SELECT DISTINCT role FROM accounts` | **role은 검증 없는 사용자 입력**(§6.2) — 코드가 아는 4개 외에 무엇이 실제로 저장돼 있는지는 읽어야만 앎. I7은 fail-closed라 blocker는 아니고, *얼마나 많은 계정이 거부되는지*를 아는 문제 | 🟡 |
| **D2** | `SHOW CREATE TABLE` ×5 | `sync({alter:true})`가 수년간 밀어낸 실제 스키마. I12 drift 방어의 기준선 | 🟡 |
| **D3** | `accounts.password` **collation** | SQL 쪽 비밀번호 비교가 대소문자를 구분하는지 — O2를 택할 때만 의미 | 🟡 |
| **D4** | 행 수 (accounts / children / signups / calendars) | `pastCalendars`에 **페이지네이션이 없음**(§2) → 오래된 계정의 응답 크기가 실제 위험인지 판단 | 🟡 |
| **D5** | 합성 계정 비율 (`non-registered-students-parent-*`) | 로그인 불가능한 계정이 통계·adapter 결과를 오염시킴 → 필터 필요 | 🟢 |

## E. 보고만 하고, 조치는 조직의 몫 (production은 동결)

이 프로젝트가 고칠 대상이 **아닙니다**. 소유자에게 알리는 것이 올바른 처리입니다.
상세는 [S42_DISCOVERY.md](S42_DISCOVERY.md) §6.

> **[2026-08-20 주석 · W-28/W-29]** 아래 E1–E4는 조직에 보낼 **보고서 초안이 이미 존재합니다** —
> [S42_SECURITY_REPORT.md](S42_SECURITY_REPORT.md)가 같은 네 건에 대한 **선언된 단일 보안 문서**입니다.
> 지금까지 이 표는 S42_DISCOVERY §6을 요약만 하고 정작 작성된 보고서를 링크하지 않아, 읽는 사람이
> 초안이 없다고 오해할 수 있었습니다. **경로 주의:** 이 문서와 같은 마이그레이션에서 해당 보고서는
> `docs/reference/org-drafts/S42_SECURITY_REPORT.md`로 이동합니다. 위 링크는 이동 **전** 경로이므로,
> 이동 단계가 끝난 뒤에는 그 경로로 읽으십시오.
>
> **발송 여부는 별도 항목입니다.** 이 문서도, 이 주석도 무엇을 발송하지 않습니다 — 대외 발송은
> 사용자의 조치이며, 보고서의 send-status는 보고서 자신이 들고 있습니다.

| # | 내용 | 심각도 |
|---|---|---|
| **E1** | 인증 없는 요청 하나로 **API 프로세스가 종료될 수 있음** (async 핸들러 + try/catch 없음 + 타입 가드 없음, Node ≥ 15) | 높음 |
| **E2** | **role 자가 할당 가능** — 등록 시 `req.body.role`을 그대로 저장, 제한은 프론트에만 | 높음 |
| **E3** | 로그인 시 **이메일과 비밀번호 해시를 stdout에 기록** (심각도는 C7에 의존) | 중간 |
| **E4** | 6자리 코드가 **이메일 인증과 비밀번호 재설정에 공용**, `Math.random`, 만료 없음, rate limit 없음 | 중간 |

---

## 지금 순서 (D-152 이후)

1. ~~**C3(DNS) 발송**~~ · **E그룹을 조직에 통지** — ~~이 둘만 지금 유효~~ → **이 줄의 절반만
   낡았습니다 (부분 주석, 전체 폐기 아님):**
   - **C3 발송 = 낡음.** **[2026-08-20 주석 · D-153]** 조직이 이미 "가능하며 통합 시점에 추가해줌"으로
     답했습니다. 그러므로 지금 발송할 DNS ask는 없습니다.
   - **E그룹 통지 = 여전히 유효.** 이 프로젝트 일정과 무관하게 **오늘 실사용자를 받는 시스템**의
     문제이므로 D-152 동결의 예외입니다(위 "아직 유효한 예외"와 동일). 보고서 초안은
     [S42_SECURITY_REPORT.md](S42_SECURITY_REPORT.md) — 마이그레이션 후 경로는
     `docs/reference/org-drafts/S42_SECURITY_REPORT.md`. 발송 자체는 사용자의 조치입니다.
2. **나머지는 전부 동결.** 현재 코드베이스 완성·테스트에 집중
3. **통합 착수를 결정하는 날**, 이 문서를 다시 열어 **A1·A2·A3 → B1·B2 측정 → A4(auth) 확정**
   순서로 진행. 그 전에는 측정하지 않음(값이 낡음)

## 통합 착수 시점에 다시 읽을 것

- [S42_DISCOVERY.md](S42_DISCOVERY.md) — 프로덕션 계약·스키마·역할·타임존 (소스 기준, 2026-08-01)
- [DECISIONS.md](DECISIONS.md) D-151(발견), D-152(순서 결정)
- ⚠️ 재검증 필요: 이 문서들은 **2026-08-01 시점의 소스** 기준입니다. `sync({alter:true})`가
  매 부팅마다 라이브 스키마를 바꿀 수 있으므로, 통합 착수 시 **배포된 빌드가 그때의 checkout과
  같은지(B2)** 부터 확인해야 합니다.
