# Local dev. The staging build sets this to the empty string on purpose (see
# .github/workflows/deploy-staging.yml), so every request is same-origin behind CloudFront -
# which is why `api/client.ts` uses `??` rather than `||`: `""` must survive as a value.
VITE_CHAT_API_URL=http://localhost:8002
